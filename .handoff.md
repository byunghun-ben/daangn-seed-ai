# Handoff — 2026-04-17T15:40 from byunghun (MacBook)

## 진행 중이던 작업

**Ralph 자율 실행 Phase 2 (planning) 중단 지점**. Epic #2 (Seed component porting) 의 하위 스코프로, 이번 run 범위는:

- **Issue #17** — `decision-matrices/composition.md`에 internal primitives 섹션 신설 (13개 primitive: marks · sheet close-buttons · sheet handles · slider internals)
- **Issue #14 cluster 1F** — `references/components/` 에 5개 data display 컴포넌트 포팅: `avatar.md` · `avatar-stack.md` · `badge.md` · `notification-badge.md` · `divider.md`

triage-analyst (Phase 1) 완료 + planning-architect (Phase 2) 완료. **plan-critic 실행 직전에 중단**.

## 완료된 것 (이번 세션)

1. **플러그인 마이그레이션 완료** — 플랫폼 스킬 → Claude Code plugin + marketplace (`.claude-plugin/marketplace.json` + `plugins/daangn-seed-ai/.claude-plugin/plugin.json` + `skills/seed/` 네임스페이스). PR #13 rebase-merge. 설치 플로우 검증 완료.
2. **README Before/After 스크린샷 추가** — `bridge-studio-web` localhost 기반 (원본 랜딩 vs `/ab/daangn` variant). `docs/images/before-main.png`·`after-daangn.png`.
3. **Harness false-positive 수정** — `scripts/test.mjs` 린트 3건 개선: alertdialog 수용, Snackbar 매처 컴포넌트 기반, BEM + data-variant 버튼 variant 신호 추가, scenario 매처 noComments + RegExp 지원. (commit `a291e75`)
4. **Icon slot doc 작성** — `references/components/icon.md` (BYO guide + slot API + 토큰 매핑). cross-link 4곳(action-button/callout/snackbar/text-field). anti-pattern #14 추가. `sync-from-seed.mjs`에 LOCAL_ONLY_COMPONENTS 세트 추가 (icon, text-field). #1 완료.
5. **#3 드라이런 배치** — 3/3 시나리오 semantic PASS 확인 후 harness 수정 커밋 (위 #3).
6. **Epic #2 재구조화** — `[EPIC] Port remaining Seed components (72 → 0)` 로 확장, 하위 이슈 #14/#15/#16/#17 신설 (Tier별 분류).
7. **Ralph Phase 0/1/2** — `.ralph/decisions.json` + `.ralph/plan.md` (329 줄, 8 stories, 2 Waves) 생성.

## 다음 단계 (맥미니에서 바로 시작)

```bash
cd ~/Documents/projects/daangn-seed-ai
git pull
claude
```

Claude Code 진입 후 바로:

```
/ralph 맥북에서 핸드오프받은 Ralph Phase 2 중단 지점에서 재개. .ralph/decisions.json + .ralph/plan.md 이미 존재. plan-critic부터 실행 → APPROVE/REJECT 판정 → Phase 3 prd.json 생성 → Phase 4 Wave 1 병렬 실행 (6 stories) → Wave 2 (2 stories) → Phase 5 review+codex → Phase 6 커밋+이슈 업데이트.
```

또는 수동으로 이어서 진행할 경우:

1. **Phase 2 재개 — plan-critic 실행**:
   ```
   Agent(subagent_type: "plan-critic", model: "opus",
     prompt: ".ralph/plan.md 검토. 8 stories, 2 Waves. APPROVE/REJECT 판정. REJECT 시 최대 3라운드로 architect 재호출 후 재평가.")
   ```

2. **Phase 3 — prd.json 생성** — plan.md의 8 스토리를 기계용 포맷으로 변환 (Wave별 실행 스케줄).

3. **Phase 4 — Wave 기반 자율 구현**:
   - Wave 1 (6 독립 스토리): W1-S1 (#17 primitives section) · W1-S2~S6 (1F 5 컴포넌트). **완전 병렬 가능** (파일 충돌 없음).
   - Wave 2 (2 통합 스토리): W2-S7 (index.md 커버리지) · W2-S8 (sync-from-seed.mjs INTERNAL_PRIMITIVES allowlist + 회귀 검증).

4. **Phase 5 — ralph-reviewer + codex 병렬 리뷰** → 3라운드 fix loop.

5. **Phase 6 — 커밋 + #17 close + #14 1F 체크 + push + ralph.jsonl + 텔레그램**.

## 핵심 결정사항

1. **Epic #2 scope 분할** — 72 컴포넌트를 Tier 1(31) / Tier 2(18) / Tier 3(8, decision needed) / Primitives(13)로 분류. 사용자 선호에 따라 Ralph run 단위를 **클러스터 단위**로 짧게 유지 (단일 run에 72개 X).
2. **이번 run = #17 + 1F** — triage 분석 결과 가장 낮은 의존성 + 가장 큰 가시적 진전 (not-ported 72→54: primitives 13 + 1F 5 = 18건 감소).
3. **primitives 접근법** — 개별 `.md` 파일 없이 `composition.md` 단일 섹션 + `sync-from-seed.mjs`의 `INTERNAL_PRIMITIVES` allowlist로 처리. "직접 import 금지" 원칙 반영.
4. **deprecated primitives 3건** — `action-sheet-close-button` · `extended-action-sheet-close-button` · `menu-sheet-close-button` 는 upstream yaml에 `deprecated: No longer used` 메타 존재. composition.md에 "deprecated — 참고용만" 블록으로 분리하되 INTERNAL_PRIMITIVES에는 포함 (sync diff 노이즈 제거).
5. **Avatar namespace API** — upstream은 `Avatar.Root` / `Avatar.Image` / `Avatar.Fallback` / `Avatar.Badge` / `Avatar.Stack` 5 subcomponent namespace. avatar.md·avatar-stack.md 에서 일관되게 표현.
6. **Badge 매트릭스 압축** — 6 tone × 3 variant × 2 size = 36조합. 압축된 `Variant × Tone` 표(셀=bg 토큰)로 정리.
7. **#16 (Tier 3) 결정 보류** — manner-temp/identity-placeholder/scroll-fog 등의 port vs scope-out는 별도 run에서 사용자와 논의. 이번 run scope 밖.

## 수정된 파일 (이번 세션, 미커밋 상태)

```
A   .ralph/decisions.json       # Phase 1 triage 결과
A   .ralph/plan.md              # Phase 2 planning 결과 (329줄, 8 stories)
M   .gitignore                  # .ralph/heartbeat.*.json 추가
```

heartbeat PID 파일은 .gitignore 처리 (기기마다 다른 PID).

## 참고

- **에픽**: #2 `[EPIC] Port remaining Seed components (72 → 0)`
- **이번 run 타깃 이슈**: #17 · #14 (cluster 1F)
- **관련 sub-issues (후속 run 큐)**: #15 (Tier 2) · #16 (Tier 3, 결정 필요)
- **브랜치**: main
- **Upstream 클론 경로**: `/tmp/seed-design-sync` (맥미니에도 clone 필요 — 없으면 `scripts/sync-from-seed.mjs` 한 번 실행하면 `git clone --depth 1 https://github.com/daangn/seed-design /tmp/seed-design-sync` 자동 수행)
- **핵심 참고 파일**:
  - `_template.md` (컴포넌트 doc 포맷)
  - `icon.md` (가장 최신 완성도 높은 예)
  - `action-button.md` (yaml-backed 표준 포맷)
  - `scripts/test.mjs` (드라이런 하네스, 3 시나리오 회귀)
  - `scripts/sync-from-seed.mjs` (not-ported 트래커, INTERNAL_PRIMITIVES 확장 예정)

## 후속 run 큐 (참고)

1. ✅ (진행 중) 1F(data display) + #17(primitives)
2. 1G(feedback/loading) — inline-banner, page-banner, content-placeholder, skeleton, progress-circle, typography
3. 1A+1D(form + buttons) — checkbox, radio, switch, text-button, toggle-button, input-button
4. 1C(chips) — chip, control-chip, action-chip, chip-tab, chip-tablist
5. 1B+1E(navigation + lists) — tab, tablist, segmented-control, top-navigation, list-item, list-header, link-content
6. **#16 결정 요청 run** (사용자와 port/stub/scope-out 확정)
7. 2A(sheets)
8. 2B+2C(FAB + advanced form)
9. #16 구현 + 2D+2E(remaining Tier 2)

---
이 파일은 다음 세션에서 자동 로드된 후 삭제됩니다.
