# Handoff — 2026-04-17 (Ralph 1G run 완료 후)

## 현재 상태

**작업 없음 · clean · push 완료**. 이전 세션의 Ralph run (#14 1G) 이 완주 후 main 에 push 되었고, 이슈 정리까지 마침.

- **포팅 완료**: Tier 1 의 1F (5) + 1G (6) = 11종. InlineBanner 는 deprecated 마이그레이션 가이드로만 존재.
- `sync-from-seed.mjs` not-ported: 54 → 48
- **이번 run 결과물**: `plugins/daangn-seed-ai/skills/seed/references/components/{inline-banner,page-banner,content-placeholder,skeleton,progress-circle,typography}.md` + `index.md` 커버리지 갱신 + `which-overlay.md` deprecation 마크
- `.ralph/archive/2026-04-17-1822-1G-cluster/` 에 plan / prd / review 보존
- **Open 이슈**: #19 (1G polish follow-up), #18 (1F follow-up), #16/#15/#14 (tier 미완), #5/#4 (기존)

## 다음 세션 시작 시

```bash
cd ~/Documents/projects/daangn-seed-ai
git pull
claude
```

진입 후 바로 상황에 맞게:

### 옵션 A — #19 polish (작은 작업, 20-30분)
```
#19 처리. page-banner.md PrefixIcon 예제에 className 추가 + skeleton.md 섹션 순서 (Variants → States → Props) 1F 공통 템플릿 재확인 후 통일.
```

### 옵션 B — #18 polish (이전 1F follow-up, 20-30분)
```
#18 처리. avatar.md Props extends chain 정확성 + sync-from-seed.mjs 라벨 enum화.
```

### 옵션 C — 다음 Ralph run (클러스터 하나, 60-90분)

**1A + 1D (form controls + buttons)** — 가장 큰 비즈니스 가치.
```
/ralph #14 1A cluster (checkbox · checkbox-group · radio · radio-group · switch) + 1D (text-button · toggle-button · input-button). 8 컴포넌트.
```

**1B (navigation)**: 5 컴포넌트 (tab · tablist · segmented-control · segmented-control-item · top-navigation).
```
/ralph #14 1B cluster (tab · tablist · segmented-control · segmented-control-item · top-navigation). 5 컴포넌트.
```

**1E (lists)**: 3 컴포넌트 (list-item · list-header · link-content).
```
/ralph #14 1E cluster (list-item · list-header · link-content). 3 컴포넌트.
```

### 옵션 D — #16 결정 요청 run (대화형, 20분)
Tier 3 컴포넌트 8개 (manner-temp · identity-placeholder · scroll-fog 등) 를 포팅할지 scope-out 할지 사용자와 개별 판단.

## 주의

- `/tmp/seed-design-sync/` 는 현재 맥미니에 clone 되어 있음 (baseline SHA `1f1d21d` 기준). 다음 run 시작 시 `node scripts/sync-from-seed.mjs --json` 한 번 돌려서 upstream 최신화 권장.
- Ralph auto-cleanup 이 이전 run 흔적을 자동 archive 함 — 수동 정리 불필요.

## 참고

- **브랜치**: main (rebase-free, 혼자 쓰는 레포)
- **Closed 이번 run**: 없음 (#14 는 tier 전체 epic 이라 열려있음, 댓글로 1G 진행 상황 기록)

---
이 파일은 다음 세션 진입 후 삭제됩니다.
